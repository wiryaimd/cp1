#include<iostream>
#include<algorithm>
#include<vector>
#include<map>
#include<set>
#include<cmath>
#include<numeric>

#define debug(x) std::cerr << (#x) << " " << (x) << std::endl;
#define br std::cerr << std::endl;

using ll = long long;

int main(){

    // int t;
    // std::cin >> t;
    // for(int i = 0; i < t; i++){
    //     int n;
    //     std::string a;
    //     std::cin >> n >> a;

    //     int minn = n;
    //     for(int j = 0; j < n; j++){
    //         int cnt = 0;
    //         for(int k = 0; k < n - j - 1; k++){
    //             if(a[j] == a[j])
    //         }
    //     }
    // }

    return 0;
}