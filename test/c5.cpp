#include<bits/stdc++.h>

#define debug(x) cerr << (#x) << " " << x << endl;

using namespace std;

int main(){

    int a = 2301220;
    int n = floor(log10(a));
    cout << n << endl;

    int hoam = 23;
    debug("asdsa");
    debug(hoam);
    debug(61);
    debug(23);
    debug(421);

    return 0;
}