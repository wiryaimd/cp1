#include<iostream>
#include<algorithm>
#include<vector>
#include<map>
#include<set>
#include<cmath>

#define debug(x) std::cerr << (#x) << " " << (x) << std::endl;
#define br std::cerr << std::endl;

using ll = long long;

int main(){

    // int t;
    // std::cin >> t;
    // for(int i = 0; i < t; i++){
    //     int x, y, z, k;
    //     std::cin >> x >> y >> z >> k;

    //     double s = cbrt(k);
    //     int a = floor(s);
    //     int b = ceil(s);
    //     int c = round(s);

    //     int wx = (x - a) * 2;
    //     int wy = (y - b) * 2;
    //     int wz = (z - c) * 2;
    //     long long total = wx * wy * wz;
    //     std::cout << total << std::endl;
    // }

    // debug(x);
    // debug(y);
    // debug(z);

    return 0;
}