#include<iostream>
#include<algorithm>

using namespace std;

int main(){
    
    // string s;
    // cin >> s;

    // int maxx = 0;

    // int n = s.length();
    // int num[n] = {0};
    // for(int i = 0; i < n - 1; i++){
    //     if(s[i] == '<'){
    //         num[i + 1] = max(num[i] + 1, num[i + 1]);
    //     }
    // }

    // for(int i = n - 1; i > 0; i--){
    //     if(s[i] == '>'){
    //         num[i - 1] = max(num[i], num[i] + 1);
    //     }
    // }

    

    return 0;
}