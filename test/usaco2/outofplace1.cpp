#include<iostream>
#include<algorithm>
#include<vector>
#include<map>
#include<set>
#include<cmath>

#define debug(x) std::cerr << (#x) << " " << (x) << std::endl;
#define br std::cerr << std::endl;

using ll = long long;

int main(){

    // int n;
    // std::cin >> n;

    // std::vector<int> v;
    // for(int i = 0; i < n; i++){
    //     int x;
    //     std::cin >> x;

    //     if(std::find(v.begin(), v.end(), x) == v.end()){
    //         continue;
    //     }
    //     v.push_back(x);
    // }

    // int cnt = 0;
    // int curr = v[0];
    // int pos = 0;
    // for(int i = 1; i < n; i++){
    //     if(curr > v[i]){
    //         pos = i - 1;
    //         break;
    //     }
    //     curr = v[i];
    // }

    // for(int )


    return 0;
}