#include<iostream>
#include<algorithm>
#include<vector>
#include<map>
#include<set>
#include<cmath>
#include<numeric>

#define debug(x) std::cerr << (#x) << " " << (x) << std::endl;
#define br std::cerr << std::endl;

using ll = long long;

int main(){

    // int t;
    // std::cin >> t;

    // for(int i = 0; i < t; i++){
    //     int n;
    //     std::cin >> n;

    //     int towers[n];
    //     for(int j = 0; j < n; j++){
    //         std::cin >> towers[j];
    //     }

    //     std::vector<int> dp(n + 1, 0);
    //     for(int j = 0; j < n; j++){
    //         for(int k = 1; k <= 2; k++){
    //             if(j + k <= n){
    //                 dp[j] += std::min(dp[j + k], dp[j + k]);
    //             }
    //         }
    //     }
    // }

    

    return 0;
}